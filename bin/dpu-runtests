#!/usr/bin/env python

from dpu.template import run_tests
import sys

path = '.'
workroot = "staging"
if len(sys.argv) > 1:
    path = sys.argv[1]
if len(sys.argv) > 2:
    workroot = sys.argv[2]

print "I: Running tests in %s" % (path)

run_tests(path, workroot)
